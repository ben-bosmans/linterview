{
  "name": "linterview",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:generate": "pnpm --filter @linterview/database run generate",
    "db:migrate": "pnpm --filter @linterview/database run migrate",
    "db:deploy": "pnpm --filter @linterview/database run deploy",
    "db:studio": "pnpm --filter @linterview/database run studio",
    "db:start:dev": "docker compose -f compose.dev.yaml up -d",
    "db:stop:dev": "docker compose -f compose.dev.yaml down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.8.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
